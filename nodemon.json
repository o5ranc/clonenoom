{
    "__comment": "public 안에 코드가 변경되더라도 nodemon서버를 재시작 하지 않도록 하기 위함",
    "ignore": ["src/public/*"],
    "__comment": "exec <-- nodemon 실행전 babel을 사용해 컴파일 하기위함을 알려줌",
    "exec": "babel-node src/server.js"
}